Сверь docs/checkpoints/CHECKPOINT-03.md с тем, что реально есть в файлах и в git log.
Назови расхождения, если они есть.

Исследуй три вещи и коротко скажи, что нашёл:
  — как устроен reader и endpoint /api/snapshot сейчас;
  — что лежит в data/samples/position-report.sample.json: перечисли поля верхнего уровня и вложенные, с точным регистром, как в файле;
  — как сейчас суда попадают на карту и в карточку.

Прочитай задачи B-11, B-12, B-13 в docs/tasks/SPRINT-02.md и предложи минимальный
план: сначала преобразование одного сообщения, потом сбор набора, потом кнопка
и состояния интерфейса.

Границы: файлы не редактируй, ничего не реализуй. Второй компонент карты и второй
вид карточки не планируй — значки и карточка переиспользуются. Задачи следующей
недели не планируй.