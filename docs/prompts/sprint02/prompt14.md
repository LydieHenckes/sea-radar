Сверь docs/checkpoints/CHECKPOINT-02.md с тем, что реально есть в файлах и в git log.
Назови расхождения, если они есть.

Прочитай docs/tasks/SPRINT-02.md — это задание на новую неделю.

По документации AISStream (aisstream.io/documentation) уточни: как отправляется
подписка, какая структура у сообщения PositionReport, что лежит в MetaData и в каком
формате приходит поле времени.

Предложи минимальный серверный reader, устроенный так, чтобы в тестах можно было
подать ему заранее заготовленные сообщения и управляемое время вместо настоящего
соединения.

Границы: файлы не редактируй, ничего не реализуй. Карту, суда и карточку не трогай —
они приняты. Интерфейс на этой неделе пока не меняем.