Покажи diff относительно коммита checkpoint 02, включая новые файлы.

Отдельно проверь и назови:
  — где читается ключ и куда он попадает дальше;
  — не попадает ли он в клиентский код, в тело ответа, в логи, в тексты ошибок;
  — записан ли провенанс образца.

Раздели явно три факта: соединение открылось; сообщение получено; локальный код
проверен. Не сливай их в «работает».

Проверь, что демонстрационная часть продолжает работать как раньше.

Код не меняй. Если увидишь проблему — назови, но не исправляй.
