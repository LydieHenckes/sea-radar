Покажи diff относительно коммита checkpoint 03, включая новые файлы.

Отдельно проверь и назови фактами:
  — не попадает ли ключ в клиентский код, в ответ endpoint и в логи;
  — где завершается сбор и может ли результат завершиться дважды;
  — очищаются ли таймеры при завершении по лимиту, а не только по сроку;
  — переиспользуются ли значки и карточка или появилась вторая реализация.

Сопоставь изменения с задачами B-11, B-12, B-13 — нет ли лишнего.

Раздели явно: что проверено запуском, что проверено мной вручную, что не
проверено вовсе.

Код не меняй.
