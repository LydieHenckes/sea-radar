Задача B-06 из docs/tasks/SPRINT-01.md. Подумай, прежде чем писать код — здесь легко ошибиться с жизненным циклом таймера.

Добавь движение демонстрационных судов:

  — каждые DEMO_TICK_MS = 2000 миллисекунд (константа в конфигурации) каждое судно переходит к следующей точке своего маршрута;
  — courseDeg — начальный азимут отрезка по формуле большого круга, приведённый к [0, 360): на стартовой точке — направление ко второй точке, дальше — от предыдущей точки к текущей;
  — timestamp — реальное время браузера в момент шага; на стартовой точке — момент запуска таймера;
  — тем же тиком, которым судно приходит на последнюю точку, speedKnots становится 0; курс сохраняется последним; дальше это судно не меняется;
  — карточка выбранного судна обновляется вместе с ним и читает данные оттуда же, откуда карта.
  Требования к таймеру: один тик на все суда, а не таймер на каждое судно. Таймеры не накапливаются при перерисовках и очищаются при уходе со страницы.

Границы: паузу, перемотку, зацикливание и ускорение не добавляй. Управляющих элементов не добавляй. После обновления страницы движение начинается с начала — сохранять состояние не нужно.

Критерий: суда движутся, доходят до конца маршрута и останавливаются со скоростью 0 уз; карточка открытого судна обновляется. Запусти.

Временно добавь в тик демонстрации console.count('tick') — я хочу увидеть в консоли, сколько раз он срабатывает за интервал. Больше ничего не меняй.
// если не работает или накапливается
После hot reload тик срабатывает дважды за интервал — старый таймер не очищается при перезапуске эффекта. Почини минимально: только жизненный цикл таймера, логику движения не трогай