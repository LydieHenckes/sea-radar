Задача B-07 из docs/tasks/SPRINT-01.md, первая половина.

Установи Playwright Test и настрой минимальную конфигурацию:
  — один браузер, chromium, он уже скачан;
  — команда запуска тестов в package.json;
  — автоматический старт dev-сервера перед тестами.

Границы: других тестовых фреймворков не подключай — Playwright единственный раннер в проекте. Тесты пока не пиши, примеры из шаблона не оставляй.

Критерий: команда запуска тестов существует и отрабатывает на пустом наборе тестов.
Запусти и покажи.


npx playwright test 