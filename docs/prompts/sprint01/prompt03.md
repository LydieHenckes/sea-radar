Замени стартовую страницу экраном Sea Radar по задаче B-02 из docs/tasks/SPRINT-01.md.

Подключи Leaflet только на клиенте. Слой — OpenStreetMap Standard,
https://tile.openstreetmap.org/{z}/{x}/{y}.png, атрибуция «© OpenStreetMap
contributors» видна на карте постоянно.

Район и начальный вид возьми из SPRINT-01 и вынеси в один файл конфигурации —
и район, и центр, и zoom живут только там.

Карта занимает всю доступную область экрана, корректно работает при обновлении
страницы и при изменении размера окна.

Границы: судов, карточки, данных и кнопок не добавляй. Дополнительных библиотек
для карты не ставь — только Leaflet.

Критерий: npm run build проходит без ошибок, и карта видна в браузере. Запусти оба.

Можешь для контроля использовать составленный план из docs/plan/plan-01.md
