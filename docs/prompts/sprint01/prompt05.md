Задача B-04 из docs/tasks/SPRINT-01.md. Добавь выбор судна кликом и карточку.

Карточка показывает:
  идентификатор
  название — выводится как текст, не как HTML
  координаты — через toFixed(5), хвостовые нули сохраняются: «51.00000, 1.45678»
  скорость   — округление до одной десятой без хвостовых нулей, суффикс « уз»:
               «12.3 уз», «12 уз», «0 уз»
  курс       — Math.round(courseDeg) % 360 и знак градуса: «135°»
  время      — «12:00:00 UTC»
  источник   — «Демонстрационные данные» для demo, «AISStream» для aisstream

Любое неизвестное значение — «Нет данных».

Кнопки закрытия у карточки нет. Карточка закрывается только выбором другого судна.
Повторный клик по выбранному судну и клик по карте вне судов выбор не меняют.

Порядок в панели сверху вниз: место под кнопку (появится позже), подпись источника, карточка.

Границы: кнопку загрузки, второе судно, движение и таймеры не добавляй.

Критерий: карточка соответствует id того судна, по которому кликнули; повторный клик её не закрывает. Запусти и покажи.