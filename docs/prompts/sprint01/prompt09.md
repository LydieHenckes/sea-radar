Задача B-05 из docs/tasks/SPRINT-01.md.

Добавь ещё два демонстрационных судна — demo-2 «Демо-судно 2» и demo-3
«Демо-судно 3». Вместе с demo-1 их должно стать ровно три.

Для каждого из трёх судов задай в файле конфигурации:
  — фиксированную последовательность из 8–12 точек внутри района, литералами;
  — скорость движения в узлах, литералом рядом с маршрутом.

Границы: случайной генерации точек нет, расчёта маршрутов нет, интерполяции нет.
Движение, таймеры и анимацию не добавляй — это следующая задача.

Критерий: три судна видны в районе, каждое на первой точке своего маршрута.
Запусти и покажи.

