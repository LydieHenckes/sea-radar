Задача B-07, вторая половина. Напиши браузерные тесты:

  1. на странице три элемента с атрибутом data-vessel-id;
  2. клик по судну открывает карточку, и в ней id именно этого судна;
  3. повторный клик по тому же судну карточку не закрывает.

Три обязательных требования:
  — суда ищи только по data-атрибутам, не по тексту, не по CSS-классам;
  — заблокируй запросы к тайлам карты, чтобы тесты не зависели от сети;
  — движение автоматически не проверяй — это задача следующего спринта, я проверил движение визуально.

Границы: тестов сверх этих трёх не добавляй. Production-код не меняй — если тест не проходит, скажи мне, но код не правь.

Критерий: три теста зелёные. Запусти и покажи вывод.