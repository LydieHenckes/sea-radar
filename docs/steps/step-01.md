## B-01
Минимальный проект Next.js создан и запущен.

Что добавлено:

App Router в папке app/
React + TypeScript с "strict": true
package.json с минимальными зависимостями
package-lock.json
.gitignore для Node и Next
CLAUDE.md со стеком, командами, границами недели и правилом работы с секретами
минимальная страница без шаблонных демо и примеров
Проверки:

npm run build — успешно
npm run dev — сервер запущен
http://localhost:3000 отвечает статусом HTTP 200
HTML страницы успешно возвращается
Папки docs и .claude не изменялись.

## B-02
Задача B-02 выполнена.

Изменения:

Добавлен Leaflet 1.9.4 и @types/leaflet.
Добавлен единый файл конфигурации карты app/map-config.ts:
границы: 50.75° N, 0.95° E — 51.25° N, 1.95° E;
центр: 51.00° N, 1.45° E;
zoom: 10.
Карта реализована в app/map.tsx с клиентским подключением Leaflet.
Для полного отключения SSR добавлен app/map-client.tsx.
Стартовая страница использует карту через app/page.tsx.
Карта и контейнер занимают всю доступную область в app/globals.css.
Используется OpenStreetMap Standard с постоянной атрибуцией © OpenStreetMap contributors.
Добавлена обработка изменения размера окна через map.invalidateSize().
Суда, карточки, кнопки и реальные данные не добавлялись.
Проверки:

npm run build — успешно.
npm run dev — успешно запущен.
Порт 3000 уже был занят ранее запущенным процессом, поэтому текущий запуск автоматически использовал http://localhost:3001.
http://localhost:3001 отвечает HTTP 200.

///
Порт 3000 освобождён от старого процесса.

Приложение перезапущено на:

http://localhost:3000

Проверка прошла успешно: сервер готов, страница отвечает HTTP 200.
///
Процесс на порту 3001 остановлен. Порт свободен.

Фоновый процесс на порту 3001 завершён.

## B-03
Задача B-03 выполнена.

Изменения:

Добавлен тип Vessel в types/vessel.ts ровно с указанными полями.
Создана папка components/.
В app/map-config.ts добавлено одно демонстрационное судно:
id: "demo-1"
имя Демо-судно 1
координаты внутри района
скорость 12
курс 135
source: "demo"
Создан генератор значка в components/VesselMarker.tsx:
ориентированный значок для известного курса;
круглый нейтральный значок для courseDeg: null;
атрибуты data-vessel-id и data-icon.
Судно добавляется на карту в app/map.tsx.
Добавлена подпись «Демонстрационные данные» рядом с картой.
Движение, таймеры, карточки, клики и вторые суда не добавлялись.
Проверки:

npm run build — успешно.
Уже запущенный процесс на порту 3000 остановлен.
Приложение перезапущено на http://localhost:3000.
Проверка страницы: HTTP 200.

///доработка
Код приведён в порядок:

В types/vessel.ts добавлен тип:
VesselSource = "demo" | "aisstream".
Поле source в Vessel теперь использует VesselSource.
Поле demoVessel удалено из MAP_CONFIG.
В app/map-config.ts добавлена отдельная константа:
export const DEMO_VESSELS: readonly Vessel[].
app/map.tsx теперь создаёт маркеры из DEMO_VESSELS.
Структура Vessel и список демонстрационных судов остаются типизированными.
Дополнительные поля и суда не добавлялись.

/// доработка
Конфигурация вынесена в fixture/map-config.ts.

Изменения:

MAP_CONFIG и DEMO_VESSELS теперь находятся в папке fixture.
Импорт в app/map.tsx обновлён на ../fixture/map-config.
Старый app/map-config.ts удалён.
Существующая конфигурация с двумя судами сохранена без изменений.

/// изменение модели на Opus

## B-04
Задача B-04 выполнена.

Добавлено:

выбор судна по клику на маркер;
карточка выбранного судна в components/VesselCard.tsx;
форматирование:
координаты через toFixed(5);
скорость с округлением до десятых без лишних нулей;
курс через Math.round(courseDeg) % 360;
время в формате HH:mm:ss UTC;
источник для demo и aisstream;
неизвестные значения как Нет данных;
название выводится обычным React-текстом, без интерпретации HTML;
повторный клик по судну сохраняет карточку;
клик по карте вне судна не меняет выбор;
кнопки закрытия нет;
добавлено место под будущую кнопку сверху панели;
второй демонстрационный корабль удалён из fixture, как требовалось границами задачи.
Проверки:

npm run build — успешно.
Старый процесс на порту 3000 остановлен.
Приложение запущено на http://localhost:3000.
Проверка страницы: HTTP 200.